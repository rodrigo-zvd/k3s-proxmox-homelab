# inventory.ini
[proxmox_hosts]
# Make sure to use the IP of your Proxmox host
proxmox-server ansible_host=172.20.1.3 ansible_user=root

[all:vars]
# Uncomment and set if you don't use the default SSH key (e.g., ~/.ssh/id_rsa)
# ansible_ssh_private_key_file: "~/.ssh/your_proxmox_key"